from datetime import datetime

current_date = datetime.now()

print ("Today's date is " + str(current_date.day))
print ("Today's Month is " + str(current_date.month))
print ("Today's Year is " + str(current_date.year))
print ("Today's time is " + str(current_date.hour))
